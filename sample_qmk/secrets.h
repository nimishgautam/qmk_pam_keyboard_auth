#ifndef SECRETS_H
#define SECRETS_H

// this key is just for consistency sake with the rest of the examples
// it's in a seperate file so you can exclude it from git or whatever
static const uint8_t AUTH_KEY[] = {0x10, 0x10, 0xde, 0xad}; 

#endif
